extends Control

var camera_texture: CameraTexture

#func _ready() -> void:
	##print("CAMERAS:")
	##for f in CameraServer.feeds():
		##print(f.get_name())
	#var camera = CameraServer.get_feed(0)
	#if camera:
		#camera_texture = CameraTexture.new()
		##camera_texture.camera_feed_id
		##CameraServer.
		##CameraServer.
	#else:
		#print("no camera detected")
